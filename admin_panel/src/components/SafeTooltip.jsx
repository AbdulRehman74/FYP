// File: src/components/SafeTooltip.jsx
import React from 'react';
import { Tooltip } from 'antd';

const SafeTooltip = (props) => {
    return (
        <Tooltip {...props} />
    );
};

export default SafeTooltip;
